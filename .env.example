# =============================================================================
# LLMJury credential store — copy to ".env" in the repo root and fill in values
#   cp .env.example .env
# Never commit ".env". LiteLLM reads the standard provider variables below.
# Docs: https://docs.litellm.ai/docs/providers
# =============================================================================

# --- Optional: load a non-default env file from code or shell ---
# LLMJURY_ENV_FILE=/absolute/path/to/other.env

# -----------------------------------------------------------------------------
# xAI Grok (good for external / non-Adobe models; free tier credits on console.x.ai)
# LiteLLM model ids use the "xai/" prefix, e.g. xai/grok-3-mini
# -----------------------------------------------------------------------------
XAI_API_KEY=

# -----------------------------------------------------------------------------
# OpenAI
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# Anthropic Claude
# -----------------------------------------------------------------------------
# ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# Google Gemini (AI Studio key; LiteLLM: see providers doc for exact var names)
# -----------------------------------------------------------------------------
# GEMINI_API_KEY=

# -----------------------------------------------------------------------------
# Groq (fast hosted models; free tier at https://console.groq.com/keys)
# -----------------------------------------------------------------------------
# GROQ_API_KEY=

# -----------------------------------------------------------------------------
# Optional: comma-separated default models for the workbench UI (first = primary)
# Use any LiteLLM model string, e.g. xai/grok-3-mini, groq/llama-3.1-8b-instant
# -----------------------------------------------------------------------------
# LLMJURY_DEFAULT_MODELS=xai/grok-3-mini,gpt-4o-mini

# -----------------------------------------------------------------------------
# Optional: one-shot API check — run:  python scripts/verify_setup.py --with-api
# Set to the LiteLLM model id to probe (must match a key you configured above).
# -----------------------------------------------------------------------------
# LLMJURY_VERIFY_MODEL=xai/grok-3-mini
